[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routing keeps working
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
